<% title "New Contact" %>
<div class="grid_12" >
  <p class="page-nav">
    <%= link_to "All Contacts", admin_contacts_path %>
  </p>
  <hr />
</div>
<div class="clear"></div>
<div id="forms">
  <%= form_for :dashboard, :url => admin_contacts_path do |f| %>
  	<%= render :partial => 'form', :locals => {:f => f} %>
  <% end %>
</div>
